# DDS 地产决策引擎 — 优化报告

> 日期：2026-05-30
> 范围：`app.py`（后端 Flask）+ `index.html`（前端单页）+ `.env`（密钥）+ 高德地图链路
> 方法：源码审查 + 本地实跑 + 浏览器实测（含控制台错误、AMap key 实弹验证）

---

## 一、总体评估

系统能跑通主流程（输入地块 → 本地竞品库 + 价格带 + AI 研判 + 风险/客群/去化），数据底座（DuckDB + Vault 多年份）和决策引擎（ABM/CEO 权重）是扎实的。问题集中在**三处**：地图渲染链路、前端若干渲染/交互细节、以及客群分析的呈现深度。其中地图是唯一的 P0 阻断项，其余为体验级与技术债。

健康度：后端 **良好**（本次修复后路由/LLM/日志均正常）；前端 **中等**（核心可用，细节多）；地图链路 **待修**。

---

## 二、本次会话已修复并验证

| # | 问题 | 根因 | 状态 |
|---|------|------|------|
| 1 | `启动Web.bat` 启动后 4 个 CEO 接口 404 | `app.run()` 写在 4 个 `@app.route` 之前，阻塞主线程导致后续路由从未注册 | ✅ 已修复（把 `__main__` 块移到文件末尾，实测 11 路由全注册） |
| 2 | `.bat` 乱码 + 误报 python 未找到 | UTF-8 字节被中文 Windows 的 GBK 代码页解析，乱码字节被当命令执行 | ✅ 已修复（提示全改 ASCII，加 `PYTHONUTF8`，PowerShell 单行） |
| 3 | LLM 单点、无兜底 | 仅 DeepSeek 一个后端 | ✅ 已加统一后端：DeepSeek 主用 → 失败/超时自动降级 APIYI（Haiku），含重试与 provider/延迟/token 日志（实弹验证降级链） |
| 4 | 无统一日志/异常处理 | 裸 HTML 报错页、无请求日志 | ✅ 已加全局 JSON 500 处理器 + 请求耗时日志 |
| 5 | 竞品只显示前 30、且非本地优先 | `slice(0,30)` + 仅按距离排序 | ✅ 全量列出 + 本地库 > 高德 > 房天下 排序（实测本地 57 + 联网 13 全列） |
| 6 | 风险卡浅底白字不可读 | 浅米色卡片继承了全局浅色文字 | ✅ 卡片/标题/正文/等级/缓释措施全部改深色（实测可读） |
| 7 | 拓扑沙盘圆点过大重叠 | 竞品点 r8→12 脉冲、配套 r6 | ✅ 缩小为 r4/3.5（实测明显改善） |
| 8 | 竞品字段显示 "nan" | 后端 pandas 缺失值序列化为字符串 "nan" | ✅ 前端清洗 "nan"/"None"/NaN → 空（实测 0 次出现） |
| 9 | 左侧 AI 助手头部塌成 58px 小按钮 | 该容器存在顽固的 `width:auto` 收缩为内容宽问题，所有 %/flex/stretch 写法无效，仅显式像素宽生效 | ✅ 加 JS 动态把头部宽度同步为面板宽度（折叠/展开/侧栏切换/缩放均重算，实测满宽展开） |

---

## 三、遗留问题（按优先级）

### P0 — 阻断级

**1. 高德地图渲染失败，持续降级到"矢量拓扑沙盘"**

- **证据**：
  - 用正确的 Web端 JS Key（`1b7f…`）+ 安全码（`da9ef9…`）在 `localhost` 直接 `new AMap.Map` 实测——**真实地图完整渲染**（三亚海棠区路网、村名、椰子洲岛清晰），证明 key/安全码/域名白名单**全部正常**。
  - 但 App 的 `initMap → initLoca`（Loca v2 3D WebGL）路径失败，且失败后**一路跌穿**到 `initVectorSandbox`，而不是停在能正常工作的 2D 底图。
  - 早期（误用 REST key `783dbe` 时）控制台报 `USERKEY_PLAT_NOMATCH` + Loca `addSource undefined`；换回正确 key 后 USERKEY 已消除，但 3D/Loca 路径仍不稳。
- **根因**：降级链路设计错误。`initLoca` 抛错 → `initFallbackMap`（2D，**已证明可用**）→ 却又因某步抛错继续跌到沙盘。Loca 3D 图层是"锦上添花"，却被当成了主依赖；任何一环失败就放弃整张地图。
- **建议（见第四节详解）**：把地图改为"**基础 AMap.Map 必出 + Loca 图层可选**"。基础底图 + 标注（竞品/配套/地块中心）走已验证可用的路径，永远先渲染；Loca 仅作增强，`try/catch` 包裹，失败只是少了光效，绝不回落沙盘。

### P1 — 体验级

**2. 客群分析不够全面**
- 现状：`decision_full` 里有 `abm_market_agent`、`migration_agent`、`top_personas`，但前端 `客群画像` 区呈现较浅，缺少人群结构的可读拆解。
- 建议：把 ABM 产出展开为——主力客群画像卡（年龄/家庭结构/购买动机/可承受总价区间/对应户型）、MNL 效用敏感度（价格/面积/配套权重）、5 年马尔可夫迁移趋势的文字结论，而非只给分数。数据已存在，主要是**呈现层补全**。

**3. 左侧 AI Agent UI 观感差**
- 现状：宽度 bug 已修，但整体仍是"折叠条 + 朴素气泡"，与页面 Bauhaus 风格不统一；折叠态/圆钮态/浮窗态三套样式互相打架，CSS 已高度碎片化（多处 `!important` 战役补丁）。
- 建议：重构为一套清晰状态机（仅 `collapsed` / `expanded` / `floating` 三态），统一配色与圆角，消息气泡区分用户/AI，输入区固定底部；删掉历史 `!important` 堆叠，从第一性原理重写这块约 200 行 CSS。

**4. 数据质量：缺失值与字段对齐**
- `nan` 前端已兜住，但根子在后端：`report_parcel.py` 构造竞品 dict 时对 pandas NaN 直接 `str()`。建议在后端统一清洗（NaN→None），前端清洗作为双保险。
- 部分竞品开盘/交房、户型面积缺失较多，建议补数据源或在 UI 标注"数据缺失"而非留空。

### P2 — 架构与技术债

**5. 城市校验三处重复定义**：`query_local.CITY_FILES`、`app.py:ALLOWED_CITIES`、`report_parcel.py` 内联校验，新增城市需三处同步——抽成单一配置源。

**6. 4 个抓取脚本 URL 为 `TODO_REPLACE_ME_REAL_DOMAIN` 占位**：价格备案/网签成交脚本结构就绪但不可用，链路上的 L1（网签/备案）数据缺位，决策仍主要依赖 L2。

**7. 密钥硬编码散落**：高德/DeepSeek key 既在 `.env` 又散在 `gis_amap.py`、`generate_cloned_history.py`、`start-dds-deepseek.ps1`——统一收敛到 `.env` + 环境变量读取。

**8. `index.html` 单文件 ~233KB**：内联 CSS/JS 高度耦合、多轮"战役"补丁叠加，可维护性下降。中期建议拆分样式/脚本，建立组件边界。

**9. 编码纪律**：面向 Windows 的产物坚持 ASCII（脚本提示）/显式 UTF-8（读写），已形成约定。

---

## 四、根因深析：地图链路

**现状调用链**
```
initMap → 注入 maps.js → _loadLocaScript → initLoca(3D+Loca)
   ├─ Loca 初始化/22s 超时 → initFallbackMap(2D 纯标注)
   └─ initFallbackMap 再抛错 → initVectorSandbox(离线沙盘)
```

**问题**：3D + Loca WebGL 是最脆的一环（依赖显卡/驱动/Loca 与 maps 版本匹配/数据源 addSource 时序），却被放在主路径首位；一旦它在某些环境抛错，错误会顺着 `catch` 一路传染到沙盘，导致**即使基础地图完全可用，用户也只看到沙盘**。

**实测对照**：我用同一把 key 走"基础 `new AMap.Map`（不挂 Loca）"——**稳定渲染**。说明问题不在 key、不在网络、不在白名单，而在 **Loca 3D 这一层 + 跌穿式降级**。

**修复方案（推荐，约半天）**
1. `initMap` 成功加载 `maps.js` 后，**先无条件渲染基础 `AMap.Map` + 竞品/配套/地块中心标注**（即把现 `initFallbackMap` 提为主路径，已验证可用）。
2. 基础底图就绪后，**再尝试** Loca 光效图层，整段 `try/catch`：成功则锦上添花，失败仅 `console.warn`，**不影响已渲染的底图**。
3. 仅当 `maps.js` 脚本本身加载失败（真离线）时才进沙盘。
4. 保留 3D `viewMode/pitch` 的酷炫观感（基础 Map 同样支持 3D，不依赖 Loca）。

**收益**：地图从"经常沙盘"变为"几乎必出真实地图"，沙盘回归其本意——纯离线兜底。

---

## 五、优化路线图

| 阶段 | 事项 | 优先级 | 预估 |
|------|------|--------|------|
| 第一批 | 地图改"基础底图必出 + Loca 可选" | P0 | 0.5 天 |
| 第一批 | 后端 NaN 统一清洗（report_parcel） | P1 | 0.5 天 |
| 第二批 | 客群画像呈现补全（复用现有 ABM 数据） | P1 | 1 天 |
| 第二批 | AI Agent 面板 CSS 重写为三态状态机 | P1 | 1 天 |
| 第三批 | 城市校验单一配置源、密钥收敛 | P2 | 0.5 天 |
| 第三批 | 占位抓取脚本接真实政府域名（L1 数据） | P2 | 视数据源而定 |
| 第三批 | index.html 拆分样式/脚本 | P2 | 1–2 天 |

---

## 六、结论

阻断项只有一个——地图，且根因已定位清楚（Loca 3D + 跌穿式降级，而非 key 问题），按第四节方案改造即可让真实地图稳定显示。其余为呈现深度与技术债，按路线图分批推进即可。本次会话已把启动、路由、LLM 兜底、日志、竞品列表、对比度、点位、nan、AI 面板宽度九项落地并验证；建议下一步优先地图改造与客群呈现补全。
