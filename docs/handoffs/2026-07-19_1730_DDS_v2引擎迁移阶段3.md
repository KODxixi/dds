# Handoff: DDS_v2 v4 引擎迁移(阶段3 收尾卡点已定位)

> **状态：历史 handoff。** 其中的阶段卡点和 stub 描述已被后续实现改变；恢复工作时以 2026-07-23 的 portable handoff 和当前代码为准。

- Saved: 2026-07-19T17:30:00+08:00
- Project: D:\Vault-assets\AI_Projects\DDS_v2（引擎源迁移自 D:\Vault-assets\AI_Projects\DDS）
- Branch/revision: DDS_v2 未纳入 git（新建项目）；老 DDS 分支 codex/cesium-decision-field @ dfe544d

## 战略背景（务必先读，这是整件事的"为什么"）
经过一轮顾问式深挖，DDS_v2 的定位已**收敛锁定**：
- **一句话定义**：输入一个地块，DDS 自主产出一份"市场事实→产品主张→价值兑现"三段咬合、每个数字可溯源、达"济南报告"级别的**建筑师前策报告**；强排几何深化外包给下游 CAD/GH agent，DDS 只给它喂"定位+约束+论证"。
- **甲方资料 : DDS自主产出 = 2:8**，目标是"无甲方资料也能出济南级报告"。
- **V1 只做 3 个核心板块（其余7板块降级为占位/有边界推演，不铺开）**：
  1. **⑤产品定位/户型配比/总图强排逻辑（重中之重）** — C 方案·双模式：
     - 模式1：甲方已有强排/总图 → DDS 做分析校验
     - 模式2：无方案 → DDS 给落位建议 + 概念示意（标注"非比例概念图·需设计院深化"）
  2. **③市场竞品对标** — 用 636 城真实挂牌库做事实地基，提升置信度
  3. **⑥溢价测算** — 价值兑现出口
- **⑤的结论一律标注"分析推断级(analysis_inference)"，不伪装成实测(observed_fact)** —— 防脑补底线。
- **交付形态** = arch-front-html skill 的 v4 HTML 报告（Apple Liquid Glass），不是之前那个简单查询页。

## Goal
迁移老 DDS 验证过的 v4 报告引擎进 DDS_v2 干净分层，逐层搬+每步"与老 DDS 逐字节一致"验证，不脑补重写。最终让 arch-front-html skill 的 bridge 指向 DDS_v2 跑通。6 阶段。

## Completed（已验证）
- **阶段1 ✅ ALL_PASS**：4 个纯契约文件（evidence_contract/report_chart_contract/report_diagram_contract/report_structure_contract）迁入 `DDS_v2/src/dds/engine/`。normalize/resolve/manifest/section 输出与老 DDS **逐字节一致**。
- **阶段2 ✅ ALL_PASS**：`report_document.py`(3940行/165KB 编译器)迁入。同一 report_seed 编译，**40 个顶层字段全一致、完整语义逐字节相同**，schema=dds.report-document/1.2。
- **阶段3 ✅ ALL_PASS（2026-07-20 收官）**：`build_project_report.py`(71.6KB) + `dds_cinematic_deck.py`(37.3KB) 已迁入 engine/。`_compiler_fingerprint` 硬路径 bug 已修（详见下方「阶段3 收官证据」）。ready 包(小镇商住)双边 doc_sha 逐字符一致、partial 包(襄阳)双边异常一致，8/8 检查全绿。
- **阶段4 ✅ ALL_PASS（2026-07-20 收官）**：`report_delivery_evidence.py`(27.6KB) 迁入 engine/（sha256 逐字节一致，纯标准库零 scripts 依赖，无硬路径无需改）。冻结→校验链等价验证 9/9 全绿（详见下方「阶段4 收官证据」）。

engine/ 现有 9 文件：__init__.py / evidence_contract.py / report_chart_contract.py / report_diagram_contract.py / report_structure_contract.py / report_document.py / build_project_report.py / dds_cinematic_deck.py / report_delivery_evidence.py

## Decisions
- **迁移而非重写**：契约层+编译器都是纯逻辑（零 scripts 依赖或仅依赖已迁契约），PowerShell 直接 Copy 原文件（DDS_v2 在文件工具 scope 外，用 shell 绕过）。
- **验证黄金标准 = 确定性哈希/语义逐字节对齐**：每搬一层，用老 DDS 输出当基线比对。
- cinematic 对"bridge 契约"非必需（compile_frozen_package 只调 build_report_document），但对"完整 HTML 报告产出"必需，故迁移。

## Current state — 阶段3 精确卡点（下一步就修这里）
**根因**：`DDS_v2/src/dds/engine/build_project_report.py:1023` 硬编码：
```python
"dds_cinematic_deck.py": root / "scripts" / "dds_cinematic_deck.py",
```
这是 `_compiler_fingerprint()` 算编译器指纹时读物理文件路径。迁到 engine/ 后不存在 `scripts/` 子目录，导致用 ready 包(小镇商住)跑 compile 时报 `FileNotFoundError: ...DDS_v2\src\dds\scripts\dds_cinematic_deck.py`。
- 同类硬路径可能还有：L1022 附近的 report_document.py / 模板 / data 路径（`_compiler_fingerprint` 读多个文件算指纹），**需一并核查 L1017-1035 整段**。
- L134/L1331 的 `from scripts.report_document import` 是 try/except 的 except 分支，top-level 已命中，无害。
- L1425/L1658 的 `from scripts.dds_cinematic_deck` 同理是 except 分支。

**修法建议**：把 `_compiler_fingerprint` 里的 `root/"scripts"/"xxx.py"` 改为指向 engine 目录自身（如 `Path(__file__).resolve().parent / "xxx.py"`），或让它对缺失文件容错。注意：改指纹路径可能导致 compiler_fingerprint 值变化，`_validate_frozen_package` 会校验包内 fingerprint 与当前是否一致——老 DDS 生成的 ready 包(小镇商住)带的是老 fingerprint，迁移版重算会不同。**验证等价性时应比对"两版对同一包的行为"，而非要求 fingerprint 等于老包内值**（老包内值本就是老环境算的）。

### ⚠️ 2026-07-19 复核补充（restore 时新查证，handoff 原文遗漏）
`_compiler_fingerprint`（当前实际行号 **L1017-1036**，root=`Path(__file__).resolve().parents[1]` 即 `src/dds`）读的是 **4 个** 物理文件，逐一实测：
| 指纹组件 | 老 DDS | DDS_v2 | 结论 |
|---|---|---|---|
| `report_document.py` | `scripts/` ✅ | `engine/` ✅ | 已迁，路径写错（L1022 写 `root/"scripts"`） |
| `dds_cinematic_deck.py` | `scripts/` ✅ | `engine/` ✅ | 已迁，路径写错（L1023 写 `root/"scripts"`） |
| `dds_report_apple_16x9.html` | `templates/` ✅ | **缺失** ❌ | **未迁入 DDS_v2**（L1024-1026） |
| `report_template_profile_v1.json` | `data/` ✅ | **缺失** ❌ | **未迁入 DDS_v2**（L1027-1029） |

**修正后的阶段3 修法（比原 handoff 多两步）**：
1. 先把老 DDS 的 `templates/dds_report_apple_16x9.html`、`data/report_template_profile_v1.json` 迁进 DDS_v2（建议 `src/dds/engine/assets/` 或保留 `templates/`+`data/` 布局），否则改对 L1022-1023 后指纹仍会因缺 html/json 报 `FileNotFoundError`。
2. 再把 L1021-1029 的 4 个 `component_paths` 全部改为指向迁入后的真实位置（或对缺失容错）。
3. 重跑 `verify_stage3b.py`。
> 老 DDS 源路径（已实测存在）：`DDS\scripts\{report_document,dds_cinematic_deck}.py`、`DDS\templates\dds_report_apple_16x9.html`、`DDS\data\report_template_profile_v1.json`。

### ✅ 2026-07-20 阶段3 收官证据（已修+已验，替代上方「卡点」，仅作历史留存）
**实际执行（最小改动）**：
1. 迁入两个缺失文件到 DDS_v2 天然位置（与 `TEMPLATE_PATH`/指纹 root=`parents[1]`=`src/dds` 对齐）：
   - `DDS\templates\dds_report_apple_16x9.html` → `DDS_v2\src\dds\templates\`（sha256 MATCH，逐字节一致）
   - `DDS\data\report_template_profile_v1.json` → `DDS_v2\src\dds\data\`（sha256 MATCH，逐字节一致）
2. 只改 `build_project_report.py` 指纹 py 两行：`root/"scripts"` → `root/"engine"`（L1022-1023）。html/json 两行不动（迁入后路径已真实存在）。
   - **关键认知**：`_compiler_fingerprint.components` 只对文件内容做 `sha256`，**路径不入 hash**。故迁入内容一致 ⇒ 指纹值与老版完全相同，等价性验证更硬。
3. 附带发现 `dds_cinematic_deck.py:43-47 TEMPLATE_PATH` 也指向 `src/dds/templates/dds_report_apple_16x9.html` 且 L942 渲染真实读它——迁入后此链一并打通（阶段5 产 HTML 报告需要）。

**验证证据（新==老，全绿）**：
- 指纹等价：new fingerprint = old fingerprint = `3e9d185360fe22f7422b68700f209e533236dfcc85f22b583b6278f8290f9522`，4 组件 sha256 逐字节一致。
- verify_stage3b 8/8 PASS：
  - ready(小镇商住) 双边 doc_sha = `369e031b84b1cfd707fca754d4e05909c9296034f666db1feb0a0b6c64b53a0e`（逐字符一致，error=None）
  - partial(襄阳) 双边异常一致 = `FrozenEvidencePackageError: frozen EvidencePackage status is not compilable: 'partial'`

**⚠ 测试包真实路径修正**（原 handoff L51 少一层 `DDS\`）：
- ready = `d:\Vault-assets\AI_Projects\DDS\实际项目\Test_小镇商住\work\evidence_package.json`（133.3MB）
- partial = `d:\Vault-assets\AI_Projects\DDS\实际项目\Test_襄阳\work\evidence_package.json`（30MB）

**⚠ 环境坑（新对话务必知道）**：DDS_v2 的 `src\` 中间层在 VM 文件系统里**无法被枚举**（`os.listdir(src)` 报 WinError 3，ripgrep 对 engine 下文件报 IO error），导致 `import dds.engine.*` 的 finder 失败（`find_spec('dds')=None`）。但 `src\dds\` 及更深层可正常访问。**规避法**：验证时把 `engine\templates\data` 复制到临时区平铺跑（模块内部是 `from report_document import ...` 扁平 bare-import + `scripts.` except 回退，天然支持同目录平铺）。验证脚本在 `c:\Users\shiguanyu\.trae-cn\work\6a5cf0de832ff6e55bdc2080\`（compile_one.py / verify_stage3b.py / verify_fp2.py，临时区重启可能清，逻辑已记于此）。

### ✅ 2026-07-20 阶段4 收官证据（report_delivery_evidence 冻结→校验链）
**实际执行**：`DDS\scripts\report_delivery_evidence.py`(27.6KB) → `DDS_v2\src\dds\engine\`（+临时区平铺镜像），sha256 三方一致 = `3356605E...B6DE36`。
- **调查确认零改动**：imports 全标准库（hashlib/json/os/re/urllib），无 `from scripts.*`、无对 report_document/build_project_report/cinematic 的依赖；body 无 `scripts/`/`templates/`/`data/`/`__file__` 硬路径，所有 `Path()` 都是运行时传入的 project_dir/work/inbox。**阶段3 那种硬路径 bug 在此文件不存在，未改一行**。
- 公共入口 3 个：`validate_report_delivery_evidence`(L499) / `require_report_delivery_validation`(L661) / `verify_report_delivery_validation`(L683)。网络走 `_fetch_web_bytes`(L271，可注入 fetcher)。

**验证证据（新==老，9/9 全绿）**——用真实样本 `Test_小镇商住\work\evidence_candidate_batch.json`(62.4KB)，注入确定性 stub fetcher 消除网络变量：
- freeze: new/old status 均 `validated_for_report_delivery`，validation_hash 逐字符一致 = `d0db60849317e9505c0e2d69060973d6373723f14f1bf80cda4ad510a90fe1f5`，full_output_sha 一致。
- verify 链 4 组合全 True，**含跨版本互操作**：new-verify(old-freeze)=True、old-verify(new-freeze)=True → 冻结产物在两版间可互换。
- 脚本：`delivery_one.py` / `chain_one.py` / `verify_stage4.py`（临时区）。
> ⚠ 未覆盖：compile 阶段（需 evidence-package/1.0 + prior_validation）与 freeze 阶段对 web source 的真实 HTTPS 拉取未跑（用了 stub）。candidate-batch 走本地校验分支已充分证明两版逻辑等价；若阶段5/6 需完整 package 冻结，再补 evidence_package.json(136MB) 的 freeze 等价。

## Verification（已跑，有证据）
- 阶段1：`verify_stage1.py` → ALL_PASS（4 契约 normalize 输出逐字节一致）。
- 阶段2：`verify_stage2.py` → ALL_PASS（report_document 编译 40 字段+完整语义逐字节一致）。
- 阶段3：`compile_one.py`(子进程隔离，写小结果文件避免 136MB 包管道阻塞) + `verify_stage3b.py`。当前 old 版 error=None，new 版 error=FileNotFoundError(cinematic 硬路径)。**验证脚本都在** `c:\Users\shiguanyu\.trae-cn\work\6a5c5043f166c49779f30383\`（临时区，重启后可能被清，逻辑已记录在此 handoff，可重建）。
- 真实测试包：ready=`实际项目\Test_小镇商住\work\evidence_package.json`(136MB)；partial=`实际项目\Test_襄阳\work\evidence_package.json`(30MB)。

## Remaining work
- **阶段3 收尾**：修 `_compiler_fingerprint` 硬路径（L1017-1035 核查全部 root/"scripts"/xxx），重跑 verify_stage3b 达成 ready 包双边 doc_sha 一致 + partial 包异常一致。
- **阶段4 ✅ 已完成（2026-07-20）**：`report_delivery_evidence.py` 已迁入 engine/，冻结→校验链等价验证 9/9 全绿（见「阶段4 收官证据」）。
- **阶段5（价值核心）**：接 ③⑤⑥ 主链内容引擎——③用老 query_local(DuckDB 636城，DDS_v2 已有 src/dds/data/parcel_repo.py 雏形)；⑤产品定位双模；⑥溢价。其余7板块自适应降级。要研究老 dds_decision_engine 各 agent 段如何喂进 report_document 的 decision_full。
- **阶段6**：arch-front-html skill 接线——`DDS_ROOT` 指向 DDS_v2，跑 skill 的 `test_dds_v4_bridge.py` + `audit_skill_contract.py` 全绿。skill 位置 `C:\Users\shiguanyu\skills\arch-front-html`，契约见其 `references/dds_v4_contract.md`，bridge 见 `scripts/dds_v4_bridge.py`（用 discover_dds_root 找 scripts/report_document.py，需确认它能否识别 DDS_v2 的 engine/ 布局——可能要改 discover 逻辑或加软链）。

## Risks and blockers
- **⚠ skill 的 bridge discover_dds_root 找的是 `scripts/report_document.py`**，而 DDS_v2 放在 `src/dds/engine/`。阶段6 接线时要么改 bridge 的 discover 逻辑，要么在 DDS_v2 提供兼容入口。这是阶段6 的主要未知数。
- 136MB ready 包导致验证慢，已用写小文件规避。
- DDS_v2 早前已有 V1 骨架（src/dds/{api,services,domain,data,config,main.py} + web/index.html + 一个能跑的 FastAPI 查询页 :8090）。新迁入的 engine/ 尚未与那套 V1 骨架接线——阶段5 要把 engine 报告能力接到 service 层，替换掉之前的占位 stubs。
- DDS_v2 的 report_document 依赖：pandas/duckdb 已装；纯引擎不依赖 torch。

## Next action
**阶段3、4 均已收官（2026-07-20 ALL_PASS）。阶段5 已完成向量检索选型 + PoC 验证（见「阶段5 PoC 证据」），下一步是把语义检索正式接入 parcel_repo 并推进 ③⑤⑥ 内容引擎。** 沿用规避法：验证在临时区平铺 `engine/templates/data` 跑。

### ✅ 2026-07-20 阶段5 PoC 证据（向量检索选型 + 端到端验证）
**用户意图升级**：V2 不照搬老 DDS，要在检索方式上做代际升级，用本地 LMStudio 跑最新模型。故阶段5 从"迁移"变为"选型+重建"。

**环境实测（全部已验证）**：
- **LMStudio 已在线**（localhost:1234），模型：LLM=`qwen/qwen3.6-35b-a3b`；Embedding=`text-embedding-qwen3-embedding-0.6b`(实测 **1024 维**) + `nomic-embed-text-v1.5`。
- **DuckDB 1.5.4** 已装（parcel_repo 现有底座）。**VSS 扩展 HNSW cosine 索引跑通**（`HNSW_INDEX_SCAN used: True`）。

**选型决策（第一性原理，已定）**：用 **DuckDB VSS 扩展**做向量检索，**不引入独立向量库**。
- 决定性理由：楼盘数据本就在 DuckDB，竞品检索天然需"语义近邻 + 城市/区域/价格结构化过滤"混合查询，DuckDB 一条 SQL 搞定；独立库(Chroma/Qdrant/LanceDB)要双写同步+应用层拼过滤，自找复杂度。
- 老 DDS 的向量栈是**残缺历史包袱**：`vector_evidence.py` 的 `_try_vector_backend` 恒返 None（向量从未真启用，一直走 CSV 兜底）；其余脚本依赖外部 `~/Gavis/gavis-core` LanceDB（用户已决定移除）。**V2 不迁移它，重建。**
- **关键坑 + 规避**：DuckDB VSS 的 HNSW 持久化仍 experimental（`hnsw_enable_experimental_persistence`，崩溃可能损坏索引，官方不建议生产持久化）。规避：**向量数据(FLOAT[])落盘存储，HNSW 索引服务启动时内存重建**（楼盘量级小，秒级）。
- **网络坑 + 规避**：DuckDB 进程走环境变量代理 `127.0.0.1:59527` 无法从 extensions.duckdb.org 下扩展。规避：PowerShell 清代理直连下载 `vss.duckdb_extension`(7.8MB gz→22MB) 到本地，`allow_unsigned_extensions=true` + `LOAD '<本地路径>'` 加载。扩展文件在临时区 `c:\...\6a5cf0de832ff6e55bdc2080\ext\`（重启会清，需重下）。

**端到端 PoC（杭州真实数据，ALL_PASS）**：
- 数据源：`DDS\Vault\2026新楼盘\新楼盘-杭州.csv`(2639行，868有单价)。取单价 top200 楼盘。
- 链路：楼盘语义文本 → Qwen3 embedding(200条/19秒) → DuckDB FLOAT[1024] 表 → HNSW cosine → 检索。
- **质量对比**（查询"高端改善大平层住宅，四室以上，面向改善客群"）：语义法 top6 **全是住宅+4室以上，自动排除别墅**；旧法(价格降序+户型LIKE) top1 就是别墅(凤起钱潮)，因只按价格排、匹配不到"住宅"语义意图。**语义检索价值已证实。**
- **待优化点**（非缺陷）：cosine distance 集中在 0.45-0.47 区分度偏小，因楼盘文本模板趋同——正式接线时文本构造要更突出差异化特征（区域能级/产品档次/客群定位）。
- 脚本：`emb_check.py` / `vss_check.py` / `poc_semantic.py`（临时区）。

**阶段5 正式接线建议（下一步，进行中）**：
1. 新建 `src/dds/data/vector_repo.py`：封装 embedding(LMStudio 可注入/可降级) + DuckDB 向量表 + HNSW 内存重建 + 语义检索 SQL。
2. `parcel_repo.query_competitors` 增加语义检索路径（保留旧 LIKE 作降级），接入 `analyze_parcel` 主链。**注意现有 `query_competitors:68`/`query_all_for_market:89` 的 `district LIKE '%{district}%'` 是 f-string 拼接有 SQL 注入面，接线时参数化。**
3. embedding 服务地址/模型走 settings 配置注入（默认 localhost:1234 + Qwen3-0.6b），LMStudio 不可用时降级回价格相似度，保证零配置可启动。
4. 向量落盘、HNSW 内存重建的持久化策略按上述规避法。
5. ③竞品对标接通后，再推 ⑤(abm_engine 迁移/重建)、⑥(premium_engine)——这两个引擎迁移仍面临老 DDS 的 `__file__.parent.parent` 硬路径、双分支 import、客群池降级等风险（见调查报告）。

### ✅ 2026-07-20 阶段5 接线收官证据（语义检索已接入主链，ALL_PASS）
**实际改动（4 文件，最小 diff）**：
- **新建 `src/dds/data/vector_repo.py`**：`embed_texts`(LMStudio /v1/embeddings，失败返 None) / `build_loupan_text` / `build_query_text` / `_build_city_index`(HNSW 内存构建，按 `city:行数` 缓存) / `query_semantic_competitors`(任一环节不可用返 None) / `clear_cache`。VSS 扩展加载优先读 `settings.vss_extension_path`，回退 INSTALL/LOAD。
- **`config/settings.py`**：加 `embed_base_url`(默认 http://localhost:1234/v1) / `embed_model`(Qwen3-0.6b) / `embed_enabled` / `vss_extension_path`，均可环境变量覆盖（DDS_ 前缀）。
- **`data/parcel_repo.py`**：① `query_competitors`/`query_all_for_market` 的 `district LIKE` **改参数化**(`?` + params)，修掉 SQL 注入面；② `query_all_for_market` 扩列(sub_district/address/area_range/developer/property_type)供向量文本构造；③ 新增 `query_competitors_semantic`(封装 vector_repo，不可用返 None)。
- **`services/parcel_service.py`**：竞品查询改为**语义优先→旧 LIKE 降级**；语义结果的 `similarity` 字段按 `Competitor.model_fields` 过滤后再构造；provenance 如实记录走了哪条路径 + 均相似度。

**验证证据（真实杭州数据，两场景 ALL_PASS）**——脚本 `verify_stage5_wire.py`(临时区，需先复制 dds 包到临时区跑，因 src 层无法枚举)：
- **场景1 语义在线**：query=「杭州 上城区 目标单价约60000元/㎡ 住宅竞品对标」，命中 50 条，均相似度 0.6778，provenance 显示"语义检索（DuckDB VSS + Qwen3）"。
- **场景2 embedding 断开(端口打死)**：不崩，自动降级，provenance 显示"语义检索不可用，降级本地 DuckDB LIKE"，仍命中 50 条。
- **价值印证**：同查上城区，语义法 top 命中目标价位附近住宅；降级法 top 是全区最贵(含商业/别墅)——语义更贴合竞品对标意图。

**⚠ 遗留 + 下一步**：
- 向量库目前"每进程首次查该城市时构建"，未落盘。生产化时可预构建 + 定期刷新（数据更新后调 `clear_cache`）。
- HNSW 用内存重建规避 experimental 持久化，若城市楼盘量极大需评估构建耗时（杭州 310 有价样本秒级）。
- ⑤⑥ 内容引擎(abm/premium)仍是占位 stub，未接——阶段5 只完成 ③竞品对标的语义升级，⑤⑥ 待后续。
